# Deul Balance Tracker

A simple balance tracking application that allows two users to record and update their balances in the same app. This tool provides an easy way to add and subtract values for each user without complexity.

## 🧾 Overview

**Deul Balance Tracker** is designed to be lightweight and straightforward. It’s ideal if you need a quick utility to manage simple balance totals for two users — with no database or complicated setup required.

## 🚀 Features

- Track and update balances for **two users**
- Simple interface without external dependencies
- Suitable for quick, local use
- No backend or cloud needed

## 📦 Getting Started

These instructions will help you run the app locally or explore the source code. That's if you are not looking for an APK file. If you are then check realeses for a ready made apk file.

### Prerequisites

- A C++ toolchain and build system (CMake)
- [Flutter](https://flutter.dev/) if you want to modify the Flutter UI parts
- Basic environment for building apps in your platform (Windows / macOS / Linux)

### Running the App

1. Clone the repository:

    ```bash
    git clone https://github.com/Diyaa-AKA-dhad/Deul-Balance-Tracker.git
    ```

2. Navigate to the project:

    ```bash
    cd Deul-Balance-Tracker
    ```

3. Depending on the platform part you want to build (e.g., C++ or Flutter UI), follow the respective build process in the code.

This repository includes both native code and Flutter code, so run the parts you intend to edit or use.

## 🛠 Development

If you want to expand features or customize behavior:

- Improve UI/UX (Flutter)
- Add persistent storage (e.g., using SQLite or JSON)
- Add dynamic user count instead of just two
- Add transaction history

Contributions are welcome!

## 💡 How It Works

The core idea of this app is very simple:

- Each user has a balance value
- You can add or subtract amounts to update their balance
- Balances are updated and reflected immediately

## 🤝 Contributing

If you’d like to contribute:

1. Fork the repository
2. Create a new feature branch:

    ```bash
    git checkout -b feature/YourFeature
    ```

3. Commit your changes:

    ```bash
    git commit -m "Add some feature"
    ```

4. Push to your branch:

    ```bash
    git push origin feature/YourFeature
    ```

5. Open a Pull Request

## 📄 License

This project is licensed under the **MIT License** — see the [LICENSE](LICENSE) file for details. :contentReference[oaicite:0]{index=0}

## ✍️ Author

**Diyaa AKA dhad**
